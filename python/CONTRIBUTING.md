# Guia de Contribuicao

## Fluxo recomendado

1. Atualize a `main` local com `git pull`.
2. Crie uma branch por tarefa, por exemplo: `feat/aula01-ex01`.
3. Faça commits pequenos e descritivos.
4. Abra Pull Request para revisao.

## Padrao de commits

- `feat:` nova funcionalidade
- `fix:` correcao de bug
- `docs:` documentacao
- `refactor:` mudanca sem alterar comportamento

## Revisao minima antes de PR

- Executar os scripts alterados.
- Conferir se os nomes e saidas seguem o enunciado.
- Conferir se o `README.md` foi atualizado quando necessario.
